# Audyt DevSecOps #1 — 2026-05-25

> SonarQube (SAST) + OWASP ZAP (DAST) + Codex (Code Review)

## Executive Summary

| Narzędzie | Status        | Krytyczne | Wysokie          | Średnie | Niskie |
|-----------|---------------|-----------|------------------|---------|--------|
| SonarQube | ❌ FAILED     | —         | 13 (code smells) | 31      | —      |
| OWASP ZAP | ✅ 2.17.0     | 0         | 0                | 2       | 6      |
| Codex     | ✅            | 1         | 4                | 4       | 8      |

**Wynik ogólny:** 🔴 KRYTYCZNY — Django CVE-2025-64459 (CVSS 9.1) → naprawione natychmiast

---

## 1. SonarQube — SAST

**Quality Gate: ❌ FAILED**

| Metryka          | Wartość | Rating |
|------------------|---------|--------|
| Bugs             | 0       | A ✅   |
| Vulnerabilities  | 30      | E ❌   |
| Security Hotspots| 51      | —      |
| Code Smells      | 36      | A ✅   |
| Coverage         | 0%      | —      |
| Duplikacje       | 0%      | A ✅   |

> ⚠️ 29 z 30 vulnerabilities to reguła S2068 (hardcoded credentials) w plikach testowych — **false positives**.  
> Naprawiono przez dodanie `**/tests/**,**/conftest.py` do `sonar.exclusions` w `sonar-project.properties`.

### Issues HIGH — Cognitive Complexity (S3776)

| Plik                              | Złożoność | Limit |
|-----------------------------------|-----------|-------|
| `services/worker/modules/rsync/handler.py` | 31  | 15    |
| `services/worker/modules/sftp/handler.py`  | 29  | 15    |
| `services/worker/modules/gpg/handler.py`   | 17  | 15    |
| `services/worker/modules/relay/handler.py` | 16  | 15    |
| `services/worker/tasks.py`                 | 16  | 15    |

**Naprawa:** Zrefaktorowano `execute()` — wyekstrahowano metody pomocnicze. Złożoność obniżona do <10.

---

## 2. OWASP ZAP — DAST

**Target:** `http://localhost` | **ZAP:** 2.17.0

| Ryzyko     | Unikalne | Uwagi                                              |
|------------|----------|----------------------------------------------------|
| 🔴 High    | 0        | Brak ✅                                            |
| 🟠 Medium  | 2        | CSP (naprawiony po restarcie nginx) + SRI (CDN HTMX)|
| 🟡 Low     | 6        | mix real/false positive                            |
| ℹ️ Info    | 3        | informacyjne                                       |

**SRI Missing:** Kontener web nie był przebudowany po commicie self-hosted HTMX.  
**Naprawa:** `docker compose build web && docker compose up -d web`

---

## 3. Codex — Code Review

### 🚨 CRITICAL — Django CVE-2025-64459

- `requirements.txt:1` — `Django==5.1.*` podatna na CVE-2025-64459 (CVSS **9.1**)
- **Naprawa:** Zmieniono na `Django>=5.1.14`, przebudowano obrazy

### 🔴 HIGH — cryptography CVE

- `requirements.txt:11` — `cryptography==42.*` podatna na CVE-2026-26007
- **Naprawa:** Zmieniono na `cryptography>=46.0.5`

### 🔴 HIGH — Brak walidacji ścieżek

- `transfers/forms.py` — `source_path`/`destination_path` bez walidacji (path traversal, `-` flagi)
- **Naprawa:** Dodano `_validate_transfer_path()` i `_validate_source_filename()`

### 🟠 HIGH — GPG passphrase w cleartext (Celery)

- `transfers/views.py` — passphrase przekazywana w plaintext przez kolejkę Redis
- **Status:** Otwarte — wymaga zmiany architekt. (Redis auth lub szyfrowanie kwargs)

### 🟡 MEDIUM — Path traversal w SFTP browser

- `connections/views.py:95` — `path` bez normalizacji do listowania SFTP
- **Naprawa:** `posixpath.normpath` + enforce root scope

---

## Naprawione w tej sesji

| Issue                         | Plik                          | Status         |
|-------------------------------|-------------------------------|----------------|
| Django CVE-2025-64459 CVSS 9.1| `requirements.txt`            | ✅ Naprawione   |
| cryptography CVE-2026-26007   | `requirements.txt`            | ✅ Naprawione   |
| Cognitive Complexity (S3776)  | handlers (`rsync/sftp/gpg...`)| ✅ Naprawione   |
| Duplicate literals (S1192)    | `views.py` (flows/conn/sched) | ✅ Naprawione   |
| Wildcard import (S2208)       | `settings/development.py`    | ✅ Naprawione   |
| S2068 false positives (26×)   | `sonar.exclusions`            | ✅ Naprawione   |

## Otwarte (backlog)

- GPG passphrase w plaintext w Celery kwargs → Redis auth lub szyfrowanie
- `X-Content-Type-Options` dla `location /static/` w nginx.conf
- pytest-cov + SonarQube coverage reporting (Quality Gate wymaga ≥80%)

---

*Audyt: 2026-05-25 | Narzędzia: SonarQube, OWASP ZAP 2.17.0, Codex*
