# Audyt DevSecOps #3 — 2026-05-27

> Codex security review. SonarQube i OWASP ZAP niedostępne w tej sesji.

## Executive Summary

| Narzędzie | Status             | Krytyczne | Wysokie | Średnie | Niskie |
|-----------|--------------------|-----------|---------|---------|--------|
| SonarQube | ⚠️ NIEDOSTĘPNY    | —         | —       | —       | —      |
| OWASP ZAP | ⚠️ POMINIĘTY      | —         | —       | —       | —      |
| Codex     | ✅                 | 0         | 3       | 1       | 4      |

**Wynik ogólny:** 🟠 BRAK BLOKERÓW — HIGH #1 i #2 naprawione natychmiast (commit `949d73e`)

---

## 1. Codex — Code Review

### Znaleziska

| Ryzyko  | Plik:Linia                                       | CWE      | Opis                                                                                   | Naprawa                                                      |
|---------|--------------------------------------------------|----------|----------------------------------------------------------------------------------------|--------------------------------------------------------------|
| HIGH    | `modules/sftp/handler.py:35`                     | CWE-295  | Gdy `strict_host_key_checking=True` bez `known_host_key` — fallback do AutoAddPolicy (MITM) | Fail-closed: rzuć `SFTPTransferError('CONFIG ERROR')` zamiast AutoAddPolicy |
| HIGH    | `modules/relay/handler.py:32`                    | CWE-295  | RelayHandler ten sam problem z AutoAddPolicy                                            | Identyczna naprawa fail-closed jak w SFTP                    |
| HIGH    | `.env` (jeśli commitowany)                       | CWE-798  | Django `SECRET_KEY` w plaintext w repozytorium                                         | `.env` jest w `.gitignore` — false positive dla lokalnej instalacji |
| MEDIUM  | `templates/accounts/profile.html:70`             | CWE-79   | `innerHTML = data.error` — DOM XSS jeśli payload zawiera HTML/JS                       | Użyj `textContent` zamiast `innerHTML`                       |
| LOW     | `config/settings/testing.py:10`                  | CWE-798  | Hardcoded `SECRET_KEY` w kodzie (test settings)                                         | Ładuj z env nawet w testach lub generuj efemeryczny klucz    |
| LOW     | `config/settings/testing.py:99`                  | CWE-321  | Hardcoded `FIELD_ENCRYPTION_KEY` w kodzie                                               | Wstrzykuj przez env/fixtures                                 |
| LOW     | `config/settings/testing.py:73`                  | CWE-327  | `MD5PasswordHasher` skonfigurowany                                                      | Ogranicz MD5 do test-only guardem                            |
| LOW     | `apps/accounts/views.py:103`                     | CWE-362  | Race condition: sprawdzenie limitu tokenów API nie jest atomowe                          | `transaction.atomic()` + `select_for_update()` lub constraint DB |

**OK:** Input validation, Authorization, Command injection, Path traversal, Dependency risk

---

## Naprawione

| Issue                      | Commit     | Opis                                                              |
|----------------------------|------------|-------------------------------------------------------------------|
| CWE-295 SFTP MITM          | `949d73e`  | Fail-closed: strict mode bez klucza → `SFTPTransferError`         |
| CWE-295 Relay MITM         | `949d73e`  | Identyczna naprawa w RelayHandler                                  |

---

## Otwarte (backlog)

| CWE      | Opis                                                      | Priorytet |
|----------|-----------------------------------------------------------|-----------|
| CWE-79   | `innerHTML → textContent` w `profile.html:70`            | MEDIUM    |
| CWE-798  | Hardcoded test keys w `testing.py`                       | LOW       |
| CWE-327  | MD5PasswordHasher w testing settings                      | LOW       |
| CWE-362  | Race condition limitu tokenów API                         | LOW       |

---

## Zalecenia dla kolejnego audytu

- Uruchomić SonarQube: `docker run --rm -e SONAR_HOST_URL=<url> -e SONAR_TOKEN=<token> -v $(pwd):/usr/src sonarsource/sonar-scanner-cli`
- Uruchomić aplikację przed skanem ZAP
- Oznaczyć 29 Security Hotspotów w SonarQube UI jako reviewed
- Podłączyć pytest-cov do pipeline (SonarQube wymaga ≥80% coverage)

---

*Audyt: 2026-05-27 | Narzędzie: Codex security review*
