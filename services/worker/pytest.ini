[pytest]
# Worker tests configure Django inline via tests/conftest.py.
# pytest-django is installed but Django is bootstrapped without a settings module.
addopts = --cov=modules --cov=tasks --cov-report=xml:coverage.xml
